# Resume Prompt — post-audit M1 (stato al 2026-06-12, HEAD `1576a30`)

Copia-incolla questo prompt per riprendere esattamente da dove siamo rimasti.

---

Riprendi il lavoro sul piano audit MANTIS esattamente da dove eravamo rimasti (2026-06-11, HEAD `1576a30` su `main` e `feature/forward-demo-lab`, CI 5/5 verde).

**STATO — tutto già fatto, non rifare:**
1. **Audit completo** (2026-06-10): 98 agenti, 57/57 finding verificati, grade C+. Report: `D:\tmp\MANTIS_AUDIT_2026-06-10.md`. Memoria: `project_repo_audit_2026-06-10.md`.
2. **Cleanup + decisioni** (8 decisioni utente eseguite): evolution cluster cancellato (~9.6k LOC, provato inerte), repo privato `github.com/GitBakko/algotrader-research` (composite3), Docker fixato (pip, workers=1), logger UTF-8 (il bug dei 4444 errori pytest), Karma→Vitest, CI verde dopo 7 settimane di rosso. Memoria: `project_evolution_cleanup_2026-06-11.md` + `project_audit_decisions_2026-06-11.md`.
3. **M1 money-path SHIPPED**: 10/10 fix (M1.1-M1.10) via subagent-driven TDD con doppia review (spec + quality) per task — la review ha intercettato 9 bug veri. 20 commit `dfc7df7..1576a30`. Suite 2268/0 backend + 219 frontend. Spec/piano in `docs/superpowers/specs/2026-06-11-m1-money-path-fixes-design.md` e `docs/superpowers/plans/2026-06-11-m1-money-path-fixes.md`. Memoria: `project_m1_money_path_fixes_2026-06-11.md` (contiene il backlog follow-up).

**VINCOLI OPERATIVI (invariati):**
- Backend principale **GIÙ DI PROPOSITO** (decisione utente 2026-06-11) — NON riavviarlo. Gira solo forward-lab (Scheduled Task), che importa SOLO `src.broker` + `src.utils.config`: **mai toccare `src/broker/` senza coordinarsi**.
- Branch di lavoro `feature/forward-demo-lab`; ff su main via `git push . feature/forward-demo-lab:main && git push origin main feature/forward-demo-lab`.
- `.env` è in deny-list: leggibile/scrivibile solo via comandi `!` dell'utente (le allow-rule `Read/Edit(./backend/.env)` in settings.local.json non bastano perché la deny `Read(./.env)` di progetto risolve contro la cwd di sessione).
- Suite completa: ~19 min; MAI editare file mentre gira (race → errori fantasma).
- Pre-LIVE blocker (memo): AUTH_REQUIRED=true + SECRET_KEY non-default (già hard-fail) + WS-auth.

**PROSSIMO LAVORO (in ordine di valore):**
1. **Follow-up #1 (più urgente del backlog)**: il path legacy `_run_iteration` ha lo stesso hazard empty-book fixato in M1.4 — su fetch-failure fa fallback a `get_paper_positions()` (vuoto in DEMO/LIVE) E avvelena `open_epics` (set vuoto → finestra duplicate-entry anche con reconciler dedicato ON). Anchor: `paper_loop.py` regione ~2452-2456 (pre-M1, ora shiftato). Stesso pattern di fix di M1.4 + stub-aware open_epics.
2. **M2.1 ClosePersister unico**: estrarre la persistenza close (rotation fallback, upsert idempotente, taxonomy `close_reason` canonica) in un modulo condiviso engine+loop+reconciler; `PendingClose` fuori dal god file. Vedi audit §5 M2.1.
3. **M2.2 event-loop unblock** (SLA <30s confermato dall'utente): `predict()` in executor, cache ATR per-bar per il trailing, memo per-tick di `get_market_details`, date-filter su `get_latest_candles`. Vedi audit §5 M2.2 + sketch.
4. Follow-up minori dal backlog M1 (AlertManager su degenerate close, TP1_HIT pnl NULL, ruff debt tests/ 455, unguarded subscribers frontend).

**PROCESSO CHE HA FUNZIONATO (riusalo)**: brainstorming breve per congelare lo scope → spec in `docs/superpowers/specs/` → piano TDD in `docs/superpowers/plans/` → subagent-driven (implementer → spec-reviewer → quality-reviewer, fix-loop, un commit atomico per task) → suite completa + review finale whole-range → push + watch CI. I quality reviewer devono TRACCIARE i consumer nel codice e riprodurre empiricamente, mai speculare.

Parti chiedendomi quale scope per il prossimo round (follow-up #1 da solo è S; #1+M2.1 è M; #1+M2.1+M2.2 è L).
